package org.usfirst.frc.team2791.robot.util;

/** 
 *This class is for holding numbers that you will use often and/or want to keep track of.
 *Stuff like PID constants, Max and Min values, special shooter speeds, etc.
 *
 *Ports or wiring-related values should go to RobotMap.
 *
 *Most, if not all values should be final. And if you can, try to comment where the constant is used
 */
public class Constants {
	public static final double MAX_SPEED_INPUT = 1.0; //used in DriveWithJoystick.java //The maximum speed that the driver can ask for (1.0 is the max this should be) 
	
}
